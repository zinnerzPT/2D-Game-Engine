#pragma once

#include <iostream>

class Engine
{
private:
	int RunLogics(); // VERY PLACEHOLDER
	//SDL_Texture* LoadTexture(std::string filePath, SDL_Renderer* renderTarget);
public:
	void Start();
};

